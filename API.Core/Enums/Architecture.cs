namespace DCM.Core.Enums
{
    /// <summary>
    /// Representa uma arquitetura de sistema operacional ou hardware (ex: x86, x64, ARM).
    /// </summary>
    public enum Architecture
    {
        X86,   // 32 bits
        X64,   // 64 bits
        ARM,   // ARM architecture
        ARM64, // ARM 64 bits
        Unknown // Arquitetura desconhecida
    }
}
